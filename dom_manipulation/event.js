// events in js : => the change in the state of an abject is known as events.
// this is arise to the user interaction and from relate environments.
// it is fired to notify code of "interesting changes" that may affects code execution.
// In  javascript file event handling priorities is high than inline event handling.

// 1. mouse event (click,double-click,etc)
// 2. keyboards events(keyup,keypress,keydown)
// 3.form event(submit etc.)
// 4. print event & many more.


//  syntax of event handling ==>
                                //  node.event = () => {
                                    // handle here  }

            // example :- btn.onclick = () =>{ console.log("clicked !")} 

